const assert = require("assert");
const prime = require("./functions.js");
const raise = require();

describe("", function () {
  it("", function () {
    assert.strictEqual(prime.checkPrime(5), true);
  });
  it("", function () {
    assert.strictEqual(prime.checkPrime(6), false);
  });
});
