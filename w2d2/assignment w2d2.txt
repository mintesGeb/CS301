1. exercise 1 - 
2. page31

"A is called"
"Before B is called"
"B is called"
"Before C is called"
"C is called"
"After C is called"
"After B is called"
"After A is called"

3. and 4. are done on a piece of paper , of which the photo is uploaded to gitHub

from reading ---

4. The have mostly same uses but declarations can be read from where ever they are placed ; 
but function expressions must be written in an orderly manner because they cant be called before
they are written.

5. True

function sayHi(){alert("Hi!")}
let sayHi =function () {alert("Hi!")}
let sayHello = sayHi;
